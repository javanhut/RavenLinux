//! Fallback package sources
//!
//! This module previously contained Alpine and static binary fallbacks.
//! These have been removed in favor of AUR compatibility.
//! See the `aur` module for AUR-based fallback support.

// Module kept for backwards compatibility - can be removed entirely
// if no other code references it
