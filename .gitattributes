# =============================================================================
# RavenLinux Git Attributes
# =============================================================================
# -----------------------------------------------------------------------------
# Git LFS tracking for large binary files
# -----------------------------------------------------------------------------
# Release binaries (if we ever commit them)
*.efi filter=lfs diff=lfs merge=lfs -text
# Compressed archives
*.tar.gz filter=lfs diff=lfs merge=lfs -text
*.tar.xz filter=lfs diff=lfs merge=lfs -text
*.tar.zst filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text
# Disk images
*.iso filter=lfs diff=lfs merge=lfs -text
*.img filter=lfs diff=lfs merge=lfs -text
*.qcow2 filter=lfs diff=lfs merge=lfs -text
# Kernel and initramfs
vmlinuz* filter=lfs diff=lfs merge=lfs -text
initramfs*.img filter=lfs diff=lfs merge=lfs -text
initramfs*.cpio filter=lfs diff=lfs merge=lfs -text
# Package files
*.rvn filter=lfs diff=lfs merge=lfs -text
*.deb filter=lfs diff=lfs merge=lfs -text
*.rpm filter=lfs diff=lfs merge=lfs -text
# -----------------------------------------------------------------------------
# Text file handling
# -----------------------------------------------------------------------------
# Auto detect text files and normalize line endings
* text=auto
# Force LF line endings for shell scripts
*.sh text eol=lf
*.bash text eol=lf
# Force LF for Rust files
*.rs text eol=lf
# Force LF for TOML/YAML/JSON config files
*.toml text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.json text eol=lf
# Force LF for Makefiles
Makefile text eol=lf
*.mk text eol=lf
# -----------------------------------------------------------------------------
# Binary files (don't try to diff these)
# -----------------------------------------------------------------------------
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.ttf binary
*.woff binary
*.woff2 binary
*.pdf binary
# -----------------------------------------------------------------------------
# Linguist overrides for GitHub
# -----------------------------------------------------------------------------
# Mark generated files
Cargo.lock linguist-generated=true
# Mark documentation
*.md linguist-documentation
# Ensure Rust is properly detected
*.rs linguist-language=Rust
initramfs* filter=lfs diff=lfs merge=lfs -text
