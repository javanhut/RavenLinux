# =============================================================================
# RavenLinux CI Workflow
# =============================================================================
# Quick CI checks for pull requests and commits
# Does NOT build the kernel or ISO (that's in build.yml for releases)

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  # ===========================================================================
  # Rust Checks
  # ===========================================================================
  rust-check:
    name: Rust Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: x86_64-unknown-uefi

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-check-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-check-

      - name: Check raven-init
        run: cargo check --release
        working-directory: init

      - name: Clippy raven-init
        run: cargo clippy --release -- -W warnings
        working-directory: init
        continue-on-error: true

      - name: Check bootloader
        run: cargo check --release --target x86_64-unknown-uefi
        working-directory: bootloader

  # ===========================================================================
  # Build Test
  # ===========================================================================
  build-test:
    name: Build Test
    runs-on: ubuntu-latest
    needs: rust-check

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-uefi

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-build-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-build-

      - name: Build raven-init
        run: cargo build --release
        working-directory: init

      - name: Build bootloader
        run: cargo build --release --target x86_64-unknown-uefi
        working-directory: bootloader

      - name: Report binary sizes
        run: |
          echo "## Binary Sizes" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Component | Size |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| raven-init | $(du -h init/target/release/raven-init | cut -f1) |" >> $GITHUB_STEP_SUMMARY
          echo "| raven-rc | $(du -h init/target/release/raven-rc | cut -f1) |" >> $GITHUB_STEP_SUMMARY
          echo "| raven-boot.efi | $(du -h bootloader/target/x86_64-unknown-uefi/release/raven-boot.efi | cut -f1) |" >> $GITHUB_STEP_SUMMARY

  # ===========================================================================
  # Shell Script Checks
  # ===========================================================================
  shellcheck:
    name: Shell Script Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install shellcheck
        run: sudo apt-get install -y shellcheck

      - name: Run shellcheck
        run: |
          find scripts -name "*.sh" -type f | xargs shellcheck -S warning || true
        continue-on-error: true

      - name: Test desktop build contract (offline)
        run: python3 scripts/test-desktop-build.py

      # The kernel config is a full 216 KB .config that build-kernel.sh applies
      # verbatim, so an option that falls out of that file is an option that
      # falls out of the shipped kernel -- which is how 6.17.11-raven came to
      # ship with HARDENED_USERCOPY, both SLAB_FREELIST options,
      # INIT_ON_ALLOC_DEFAULT_ON and SECURITY_YAMA all off. This reads the
      # checked-in file and needs no kernel tree, which is why it belongs in
      # the quick job rather than in build.yml's kernel build. Not
      # continue-on-error: a dropped hardening option is a regression, not a
      # style note.
      - name: Check kernel hardening floor (offline)
        run: ./scripts/check-kernel-hardening.sh
